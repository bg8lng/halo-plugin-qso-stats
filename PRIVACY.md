# 隐私与第三方数据处理说明

> 适用插件：**通联统计（QsoStats）** · 适用版本：v1.6.0 及以上
> 最后更新：2026-09-01

本说明面向两类读者：**安装本插件的站长**（数据控制者）与**访问站点的访客**（数据主体）。
本插件为开源软件，作者不运营任何服务器，也**不会收到**你或你访客的任何数据。

---

## 1. 一句话概述

本插件默认只做**只读统计展示**，不收集任何个人信息。仅当站长开启「一键 OQRS 卡片申请」
且访客主动填写并提交表单时，插件才会把**访客邮箱、留言与所选通联记录**转发到
**站长自己配置的 Wavelog 站点**，用于处理这一次 QSL 卡片申请。

---

## 2. 涉及的数据、接收方与用途

| 数据项 | 何时产生 | 接收方 | 用途 | 是否必需 |
| --- | --- | --- | --- | --- |
| 通联统计数据（总数、DXCC、波段/模式分布、最近通联等） | 页面加载时 | 站长配置的 Wavelog 站点（读取） | 生成统计图表 | 是（插件核心功能） |
| 访客输入的**查询呼号** | 访客使用呼号查询时 | 站长配置的 Wavelog 站点（读取查询） | 按呼号检索通联记录 | 否（可关闭查询功能） |
| 访客**邮箱地址** | 访客提交 OQRS 申请时 | 站长配置的 Wavelog 站点（写入） | 站长回复 QSL 卡片申请、寄送状态通知 | 否（可关闭 OQRS） |
| 访客**留言内容**（≤ 500 字，可选） | 访客提交 OQRS 申请时 | 站长配置的 Wavelog 站点（写入） | 附加说明（如寄送地址备注） | 否 |
| 访客**选择的通联记录**（日期/时间/频段/模式/电台位置 ID） | 访客提交 OQRS 申请时 | 站长配置的 Wavelog 站点（写入） | 标记需要制作的 QSL 卡片 | 否 |
| 访客 **IP 地址**（或反向代理透传的 X-Forwarded-For / X-Real-IP） | 调用查询/OQRS 接口时 | **仅停留在本站服务器内存中**，不外发 | 频率限制与防重复提交 | 是（安全必需） |

### 关于「接收方」的重要说明

- 本插件的**唯一第三方接收方**是 **站长在插件设置中填写的 Wavelog 站点地址**。
  Wavelog 是开源自建日志软件（<https://www.wavelog.org>），通常由站长自行部署在自己的服务器上。
- 插件**不会**把数据发送给插件作者、Halo 官方、应用市场或任何统计/分析服务。
- 插件**不使用** Cookie、不做用户画像、不做跨站跟踪、不加载任何第三方广告或统计脚本。
  前端所需的 ECharts 图表库随插件本地分发，不从 CDN 拉取。
- 若站长填写的 Wavelog 地址指向第三方托管服务，则该服务商同样是接收方，
  其数据处理规则由该服务商与站长约定，站长应在自己站点的隐私政策中一并披露。

---

## 3. 保存方式与保存期限

### 3.1 在本站（Halo + 本插件）内

| 内容 | 保存位置 | 保存期限 |
| --- | --- | --- |
| Wavelog 站点地址、API Token | Halo 的插件配置（ConfigMap），Token 为密码类型字段 | 直到站长修改或卸载插件 |
| 统计 / 查询结果缓存 | **服务器进程内存**（不落盘、不入数据库） | 缓存时间（默认 300 秒）到期即失效；Halo 重启即清空 |
| 频率限制计数、重复提交指纹 | **服务器进程内存** | 计数：1 分钟 / 1 小时窗口；指纹：重复提交拦截窗口（默认 24 小时）；Halo 重启即清空 |
| 访客邮箱、留言 | **不保存**。仅在单次请求中透传给 Wavelog，随请求结束即释放 | 不适用 |

> 插件不创建任何数据库表，不写入日志文件，不落盘保存访客个人信息。
> 重复提交指纹是**单向哈希值（SHA-256）**，不可还原出邮箱原文。

### 3.2 在 Wavelog 侧

OQRS 申请一经提交，即由**站长自建的 Wavelog 站点**存储并管理，其保存期限、
访问权限与删除策略由**站长**决定，本插件无法代为管理。
通常这些申请会保留至 QSL 卡片寄送完成，站长可在 Wavelog 后台随时删除。

---

## 4. 如何关闭（站长）

在 Halo 后台 →「插件」→「通联统计」→「设置」→「展示与交互」：

| 目标 | 操作 | 效果 |
| --- | --- | --- |
| 仅关闭卡片申请，保留只读查询 | 关闭 **「启用一键 OQRS 卡片申请」** | 前台不再出现申请入口；服务端拒绝所有 `POST /qso-stats/api/oqrs`（HTTP 403），**不再向 Wavelog 发送任何访客数据** |
| 同时关闭呼号查询与卡片申请 | 关闭 **「启用呼号查询与 OQRS」**（总开关） | 前台不再出现查询框；服务端同时拒绝 `GET /qso-stats/api/search` 与 `POST /qso-stats/api/oqrs`（HTTP 403） |
| 完全停止一切数据交互 | 在「插件」列表中**停用**或**卸载**本插件 | 所有前台路由与接口下线，内存缓存立即释放 |

> **关闭即服务端生效**：这些开关不是前端隐藏，服务端会在处理请求的第一步就拒绝，
> 直接调用接口同样返回 403。详见 README 的「访问控制与防滥用」一节。

---

## 5. 如何删除数据

### 访客

- **撤回或删除已提交的 OQRS 申请**：请通过本站公开的联系方式（邮箱 / 留言板 / 关于页）联系站长，
  说明你的呼号与邮箱，站长可在 Wavelog 后台删除对应申请记录。
- 你的邮箱与留言**不会**保存在本站的 Halo 数据库中，因此无需向 Halo 侧单独申请删除。
- 用于限流的 IP 计数最长在 1 小时后自动过期，重复提交指纹最长在 24 小时（可配置）后自动过期，
  两者均不与身份信息关联，也不会持久化。

### 站长

- 删除单条申请：登录 Wavelog →「OQRS」管理页面 → 删除对应请求。
- 清空插件内存缓存：停用后重新启用插件，或重启 Halo。
- 撤销 API 访问：在 Wavelog 的 API 页面吊销该 `wl2_` Token，并清空插件设置中的 Token 字段。

---

## 6. 站长的合规义务

本插件为站长提供技术能力，**站长是访客个人信息的数据控制者**。若你开启了 OQRS 功能，建议：

1. 在站点的隐私政策中说明：收集访客邮箱与留言、用于 QSL 卡片申请、接收方为你自建的 Wavelog 站点；
2. 保留本插件前台 OQRS 表单中的**数据处理告知与勾选确认**（默认必须勾选才能提交），不要移除；
3. 妥善保管 Wavelog API Token，使用最小权限（`statistic:read`、`qso:read`）；
4. 为站点启用 HTTPS，避免访客邮箱在传输过程中被窃听；
5. 在不需要卡片申请时关闭 OQRS 开关，遵循数据最小化原则。

---

## 7. 联系方式

- 插件问题与安全报告：<https://github.com/bg8lng/halo-plugin-qso-stats/issues>
- 作者：BG8LNG · <https://bg8lng.com>

关于**某个具体站点**收集的数据，请联系该站点的站长；插件作者无法访问、也不持有这些数据。
